# Preview refinement

This branch preserves the UI from commit `0381fb5` and applies only two focused changes:

- rebalances the daily planning table column widths, giving more room to action/decision and cost while reducing unused width in short numeric columns;
- adds the forecast peak as a first-class KPI with both value and date on Dashboard and Simulation.

No merge is intended until the preview is reviewed visually.
